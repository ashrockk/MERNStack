// Send email to user.


/*
Steps Package called nodemailer
package require
nodemailer initializer i.e. transporter
 transporter.sendMail() */

 const nodemailer = require("nodemailer");

 const mailOptions ={
    from: '"Saurabh" <saurabh.karki98176@gmail.com>', // sender address
    to: "saurabh.karki9817@gmail.com,ayush.bhusal0000@gmail.com,diwaswatson@gmail.com,balkrishnaroy31@gmail.com,bikrambhusal75@gmail.com, sandipneupane775@gmail.com ", // list of receivers
    subject: "W script✔", // Subject line
    text: "Mail aayo hai aayo, JS ko script garera mail aayo!", // plain text body
    html: "<b>Hello world?</b>", // html body
  
};

const transporter = nodemailer.createTransport({
    host: "smtp.gmail.com",
    port: 465,
    secure: true,
    auth: {
      // TODO: replace `user` and `pass` values from <https://forwardemail.net>
      user: 'saurabh.karki98176@gmail.com',
      pass: '', // password use garnu, application testing lai create gareko.
    },
  });

//   transporter.on("verify", () =>{

// //console.log("verified");

//   });
const mailer = (mailOptions, transporter) => {
    console.log("Email is being sent. On the way....");


//     const info = await transporter.sendMail(mailOptions);
//     console.log({info});
// };
    transporter.sendMail(mailOptions, (err, info)=>{
    if(err){
            console.log(err);
    }else{
            console.log(info);
    }
    });
};
mailer(mailOptions,transporter);



